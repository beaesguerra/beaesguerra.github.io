import * as React from 'react';

type PlaceholderProps = {
}

const Placeholder = (props: PlaceholderProps) => (
  <div className="bg-gray-200 w-full h-full min-h-40" />
);

export default Placeholder;
