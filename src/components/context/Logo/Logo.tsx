import * as React from 'react';
import { Typography, ButtonBase } from '@material-ui/core';

export const Logo = () => {
  return (
    <ButtonBase href="/">
      <Typography variant="h4">
        bea esguerra
      </Typography>
    </ButtonBase>
  );
};
